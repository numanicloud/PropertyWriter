<UserControl x:Class="PropertyWriter.View.ClassControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	
	<UserControl.Resources>
		<ResourceDictionary Source="InlineProperty.xaml"/>
	</UserControl.Resources>
	
	<Grid>
		<DataGrid
			AutoGenerateColumns="False"
			ItemsSource="{Binding Properties}"
			GridLinesVisibility="Horizontal"
			MinRowHeight="30"
			Focusable="False">
			<DataGrid.HorizontalGridLinesBrush>
				<SolidColorBrush Color="Gray" Opacity="0.5"/>
			</DataGrid.HorizontalGridLinesBrush>
			<DataGrid.Columns>
				<DataGridTextColumn
					IsReadOnly="True"
					Binding="{Binding PropertyInfo.Name}"
					Header="プロパティ名"/>
				
				<DataGridTemplateColumn Header="値">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<ContentControl Content="{Binding Instance}" Focusable="False" Margin="0 2"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
			
		</DataGrid>
		
	</Grid>
</UserControl>
