<Window x:Class="PropertyWriter.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PropertyWriter" Height="400" Width="640">
	
	<Window.InputBindings>
		<KeyBinding Command="{Binding NewFileCommand}" Modifiers="Control" Key="N"/>
	</Window.InputBindings>
	
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		
		<Menu Height="24" Grid.Row="0">
			<Menu.Background>
				<LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
					<GradientStop Color="#FFF6F6F6" Offset="0.25"/>
					<GradientStop Color="#FFEAE8E8" Offset="0.25"/>
					<GradientStop Color="#FFDCD9D9" Offset="0.8"/>
					<GradientStop Color="#FFCFCFCF" Offset="1"/>
				</LinearGradientBrush>
			</Menu.Background>
			<MenuItem Header="ファイル">
				<MenuItem Header="新規作成" InputGestureText="Ctrl+N" Command="{Binding NewFileCommand}"/>
				<MenuItem Header="開く" InputGestureText="Ctrl+O"/>
			</MenuItem>
		</Menu>

		<ContentControl Content="{Binding Root}" Grid.Row="1" Focusable="False" Margin="8">
			<ContentControl.Resources>
				<ResourceDictionary Source="BlockProperty.xaml" />
			</ContentControl.Resources>
		</ContentControl>

	</Grid>
</Window>
