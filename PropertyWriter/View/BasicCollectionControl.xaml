<UserControl x:Class="PropertyWriter.View.BasicCollectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>

		<ListBox 
				Name="dataList"
				ItemsSource="{Binding Collection}"
				Grid.Row="0">
			<ListBox.Resources>
				<ResourceDictionary Source="InlineProperty.xaml"/>
			</ListBox.Resources>
		</ListBox>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>

			<Button Name="addButton" Content="追加" Click="addButton_Click" Grid.Column="0"/>
			<Button Name="removeButton" Content="削除" Click="removeButton_Click" Grid.Column="1"/>
		</Grid>
	</Grid>
</UserControl>
